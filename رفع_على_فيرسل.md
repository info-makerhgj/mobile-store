# 🚀 كيف ترفع المشروع على Vercel (بالعربي البسيط)

## الطريقة الأسهل - بدون GitHub

### 1️⃣ نزّل Vercel CLI
افتح Terminal (CMD) واكتب:
```bash
npm install -g vercel
```

### 2️⃣ سجل دخول
```bash
vercel login
```
راح يفتح لك صفحة في المتصفح، سجل دخول بـ Google أو GitHub

### 3️⃣ ادخل لمجلد Frontend
```bash
cd frontend
```

### 4️⃣ ارفع المشروع
```bash
vercel
```

راح يسألك أسئلة، اختر:
- **Set up and deploy?** اكتب `Y` واضغط Enter
- **Which scope?** اختر حسابك
- **Link to existing project?** اكتب `N` واضغط Enter
- **What's your project's name?** اكتب `mobile-store` واضغط Enter
- **In which directory is your code located?** اضغط Enter مباشرة
- **Want to override the settings?** اكتب `N` واضغط Enter

### 5️⃣ أضف رابط Backend
```bash
vercel env add NEXT_PUBLIC_API_URL production
```
اكتب: `http://localhost:4000/api` واضغط Enter

### 6️⃣ ارفع النسخة النهائية
```bash
vercel --prod
```

### ✅ خلاص! 
راح يعطيك رابط مثل: `https://mobile-store-xxx.vercel.app`

---

## الطريقة الثانية - من موقع Vercel مباشرة

### 1️⃣ ارفع المشروع على GitHub أولاً

```bash
# في المجلد الرئيسي (مو frontend)
git init
git add .
git commit -m "رفع المشروع"
```

روح لـ GitHub.com وأنشئ repository جديد، ثم:

```bash
git remote add origin https://github.com/اسمك/اسم-المشروع.git
git branch -M main
git push -u origin main
```

### 2️⃣ روح لموقع Vercel

1. افتح https://vercel.com
2. سجل دخول بحساب GitHub
3. اضغط **"Add New"** ثم **"Project"**
4. اختر المشروع من GitHub
5. **مهم جداً**: 
   - في **Root Directory** اختر `frontend`
   - في **Framework Preset** اختر `Next.js`
6. اضغط **"Environment Variables"**
   - اكتب: `NEXT_PUBLIC_API_URL`
   - القيمة: `http://localhost:4000/api`
7. اضغط **"Deploy"**

### ✅ خلاص!
انتظر 2-3 دقائق وراح يكون جاهز

---

## ⚠️ ملاحظات مهمة

### 1. Backend لازم يشتغل
المشروع راح يفتح بس المنتجات ما راح تظهر إلا لو:
- Backend شغال على جهازك
- أو Backend مرفوع على سيرفر (AWS مثلاً)

### 2. لو تبي Backend يشتغل
لازم ترفعه على سيرفر (شوف ملف `AWS_DEPLOYMENT_GUIDE_AR.md`)

### 3. التحديثات
لو عدلت شي في الكود:

**لو استخدمت Vercel CLI:**
```bash
cd frontend
vercel --prod
```

**لو استخدمت GitHub:**
```bash
git add .
git commit -m "تحديث"
git push
```
Vercel راح ينشر تلقائياً!

---

## 🆘 مشاكل؟

### المشكلة: Build فشل
جرب تبني المشروع عندك أولاً:
```bash
cd frontend
npm install
npm run build
```
إذا اشتغل، معناها المشكلة في Vercel

### المشكلة: الصفحة بيضاء
- افتح F12 في المتصفح
- شوف Console
- غالباً المشكلة في API URL

### المشكلة: المنتجات ما تظهر
- تأكد Backend شغال
- تأكد رابط API صحيح في Environment Variables

---

## 📞 محتاج مساعدة أكثر؟

شوف الملفات:
- `VERCEL_QUICK_DEPLOY.md` - تفاصيل أكثر
- `AWS_DEPLOYMENT_GUIDE_AR.md` - لرفع Backend
- `VERCEL_DEPLOYMENT_AR.md` - دليل كامل

---

**بالتوفيق! 🎉**

أي سؤال، اسأل!
