# دليل اختبار الإصلاحات 🧪

## قبل البدء

تأكد من تشغيل:
```bash
# Terminal 1 - Backend
cd backend
npm run dev

# Terminal 2 - Frontend  
cd frontend
npm run dev
```

---

## 1️⃣ اختبار صفحة البروفايل

### الخطوات:
1. افتح المتصفح على: `http://localhost:3000`
2. اضغط على "تسجيل الدخول" أو اذهب إلى: `http://localhost:3000/login`
3. سجل دخول بحساب عادي (ليس أدمن)
4. اذهب إلى: `http://localhost:3000/account`

### ما يجب أن تراه:
- ✅ صفحة البروفايل تظهر بشكل كامل
- ✅ اسم المستخدم وبريده الإلكتروني
- ✅ قائمة جانبية بالتبويبات:
  - طلباتي
  - المفضلة
  - العناوين
  - الملف الشخصي
  - الإعدادات
  - تسجيل الخروج

### اختبر التبويبات:

#### تبويب "طلباتي"
- ✅ يعرض قائمة الطلبات (أو رسالة "لا توجد طلبات")
- ✅ كل طلب يظهر:
  - رقم الطلب
  - التاريخ
  - الحالة (قيد المعالجة، قيد الشحن، تم التوصيل)
  - المبلغ الإجمالي

#### تبويب "الملف الشخصي"
- ✅ يعرض بيانات المستخدم
- ✅ زر "تعديل" يظهر
- ✅ عند الضغط على "تعديل":
  - الحقول تصبح قابلة للتعديل
  - يظهر زر "حفظ" و "إلغاء"
- ✅ عند الضغط على "حفظ":
  - يتم حفظ التغييرات
  - رسالة نجاح تظهر

#### تبويب "العناوين"
- ✅ يعرض قائمة العناوين
- ✅ زر "إضافة عنوان جديد"
- ✅ كل عنوان يظهر:
  - الاسم (المنزل، العمل، إلخ)
  - العنوان الكامل
  - رقم الجوال
  - علامة "افتراضي" إن وجدت

#### تبويب "الإعدادات"
- ✅ خيارات الإشعارات
- ✅ زر "حذف الحساب"

---

## 2️⃣ اختبار صفحة إعدادات الدفع (أدمن)

### الخطوات:
1. افتح المتصفح على: `http://localhost:3000/admin/login`
2. سجل دخول بحساب أدمن
3. اذهب إلى: `http://localhost:3000/admin/settings/payment`

### ما يجب أن تراه:
- ✅ صفحة إعدادات الدفع
- ✅ 4 طرق دفع:
  1. تاب للدفع (Tap Payment)
  2. تابي (Tabby)
  3. تمارا (Tamara)
  4. مدفوع (MyFatoorah)

### اختبر كل طريقة دفع:

#### تفعيل طريقة دفع
1. اضغط على المفتاح (Toggle) بجانب أي طريقة
2. ✅ يجب أن يتحول إلى اللون الأخضر
3. ✅ تظهر حقول إدخال المفاتيح السرية

#### إدخال المفاتيح
1. أدخل قيم تجريبية في الحقول
2. ✅ الحقول السرية (API Key, Secret Key) تكون مخفية
3. ✅ زر العين (👁️) يظهر بجانب الحقول السرية
4. اضغط على زر العين
5. ✅ النص يظهر/يختفي

#### حفظ الإعدادات
1. اضغط على زر "حفظ التغييرات"
2. ✅ يظهر "جاري الحفظ..."
3. ✅ رسالة نجاح تظهر باللون الأخضر
4. ✅ الإعدادات محفوظة

#### التحقق من الحفظ
1. أعد تحميل الصفحة (F5)
2. ✅ الإعدادات لا تزال موجودة
3. ✅ طرق الدفع المفعلة لا تزال مفعلة

---

## 3️⃣ اختبار صفحة العملاء (أدمن)

### الخطوات:
1. من لوحة تحكم الأدمن
2. اذهب إلى: `http://localhost:3000/admin/customers`

### ما يجب أن تراه:
- ✅ قائمة العملاء
- ✅ إحصائيات في الأعلى:
  - إجمالي العملاء
  - العملاء النشطين
  - إجمالي الطلبات
  - إجمالي الإيرادات
- ✅ شريط بحث
- ✅ فلتر (جميع العملاء، نشطين، غير نشطين)

### اختبر البطاقات:
كل بطاقة عميل تحتوي على:
- ✅ صورة رمزية (Avatar)
- ✅ الاسم
- ✅ البريد الإلكتروني
- ✅ رقم الجوال
- ✅ عدد الطلبات
- ✅ إجمالي الإنفاق
- ✅ الحالة (نشط/غير نشط)
- ✅ زر "عرض التفاصيل"
- ✅ زر إرسال بريد

---

## 4️⃣ اختبار الفصل بين الصفحات

### التأكد من الفصل الصحيح:

#### كمستخدم عادي:
1. سجل دخول كمستخدم عادي
2. حاول الوصول إلى: `http://localhost:3000/admin`
3. ✅ يجب أن يتم رفض الوصول أو إعادة التوجيه

#### كأدمن:
1. سجل دخول كأدمن
2. اذهب إلى: `http://localhost:3000/account`
3. ✅ يجب أن تعمل الصفحة (الأدمن أيضاً مستخدم)
4. ✅ تعرض بيانات الأدمن الشخصية فقط

---

## 5️⃣ اختبار API

### اختبار جلب إعدادات الدفع:
```bash
# استبدل YOUR_ADMIN_TOKEN بالتوكن الفعلي
curl -X GET http://localhost:5000/api/payments/settings \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "settings": {
    "tap": {
      "enabled": false,
      "config": {}
    },
    "tabby": {
      "enabled": false,
      "config": {}
    },
    "tamara": {
      "enabled": false,
      "config": {}
    },
    "myfatoorah": {
      "enabled": false,
      "config": {}
    }
  }
}
```

### اختبار تحديث إعدادات الدفع:
```bash
curl -X PUT http://localhost:5000/api/payments/settings \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "tap",
    "enabled": true,
    "config": {
      "apiKey": "test_key_123",
      "secretKey": "test_secret_456"
    }
  }'
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "message": "تم تحديث إعدادات الدفع بنجاح"
}
```

---

## 6️⃣ اختبار الأخطاء

### اختبار بدون تسجيل دخول:
1. افتح نافذة تصفح خفي (Incognito)
2. اذهب إلى: `http://localhost:3000/account`
3. ✅ يجب إعادة التوجيه إلى `/login`

### اختبار صفحة أدمن بدون صلاحيات:
1. سجل دخول كمستخدم عادي
2. اذهب إلى: `http://localhost:3000/admin/settings/payment`
3. ✅ يجب رفض الوصول

---

## 7️⃣ اختبار التجاوب (Responsive)

### على الجوال:
1. افتح أدوات المطور (F12)
2. فعّل وضع الجوال (Device Toolbar)
3. اختبر الصفحات على أحجام مختلفة:
   - iPhone SE (375px)
   - iPhone 12 Pro (390px)
   - iPad (768px)
   - Desktop (1920px)

### ما يجب أن تراه:
- ✅ القوائم الجانبية تتحول إلى قوائم منسدلة
- ✅ الجداول تصبح قابلة للتمرير
- ✅ الأزرار بحجم مناسب للمس
- ✅ النصوص واضحة وقابلة للقراءة

---

## 8️⃣ اختبار الأداء

### سرعة التحميل:
1. افتح أدوات المطور (F12)
2. اذهب إلى تبويب Network
3. أعد تحميل الصفحة
4. ✅ الصفحة يجب أن تحمل في أقل من 2 ثانية

### استهلاك الذاكرة:
1. افتح أدوات المطور (F12)
2. اذهب إلى تبويب Performance
3. سجل أداء الصفحة
4. ✅ لا توجد تسريبات في الذاكرة (Memory Leaks)

---

## 9️⃣ اختبار المتصفحات

اختبر على:
- ✅ Chrome
- ✅ Firefox
- ✅ Safari
- ✅ Edge

---

## 🔟 قائمة التحقق النهائية

### Frontend
- ✅ صفحة البروفايل تعمل
- ✅ صفحة إعدادات الدفع تعمل
- ✅ صفحة العملاء تعمل
- ✅ لا توجد أخطاء في Console
- ✅ التصميم متجاوب
- ✅ الأزرار تعمل
- ✅ النماذج تحفظ البيانات

### Backend
- ✅ API تستجيب بشكل صحيح
- ✅ المصادقة تعمل
- ✅ الصلاحيات محمية
- ✅ قاعدة البيانات تحفظ البيانات
- ✅ لا توجد أخطاء في Terminal

### الأمان
- ✅ المستخدم العادي لا يصل لصفحات الأدمن
- ✅ الأدمن فقط يصل لإعدادات الدفع
- ✅ المفاتيح السرية مخفية
- ✅ التوكنات محمية

---

## 🎉 النتيجة النهائية

إذا نجحت جميع الاختبارات:
**✅ المشروع يعمل بشكل كامل وجاهز للاستخدام!**

إذا فشل أي اختبار:
1. راجع ملف `FIXES_COMPLETE.md`
2. تحقق من ملفات `.env`
3. تأكد من تشغيل MongoDB
4. افحص Console و Terminal للأخطاء
