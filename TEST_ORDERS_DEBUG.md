# 🔍 تشخيص مشكلة الطلبات

## المشكلة
- رقم الطلب يظهر غلط
- اسم العميل يظهر "عميل" بدل الاسم الحقيقي
- الحالة تظهر بالإنجليزي بدل العربي

## الحل المقترح

### 1. تحديث صفحة الطلبات لعرض البيانات الصحيحة

الكود الحالي يستخدم:
```tsx
order.id?.slice(-6)  // رقم الطلب
order.shippingAddress?.name  // اسم العميل
order.status  // الحالة
```

لكن البيانات القادمة من API قد تكون بصيغة مختلفة:
```tsx
order._id  // بدل order.id
order.user?.name  // بدل order.shippingAddress?.name
```

### 2. خطوات الإصلاح

1. افتح Console في المتصفح (F12)
2. اذهب لصفحة الطلبات
3. شوف البيانات المطبوعة في Console
4. تأكد من شكل البيانات

### 3. الكود المحدث

```tsx
// رقم الطلب - جرب كل الاحتمالات
const orderNumber = order.orderNumber || 
                    `#${order._id?.slice(-6)}` || 
                    `#${order.id?.slice(-6)}` || 
                    '#000000'

// اسم العميل - جرب كل الاحتمالات  
const customerName = order.user?.name || 
                     order.customer?.name ||
                     order.shippingAddress?.name || 
                     order.customerName ||
                     'عميل'

// رقم الجوال
const customerPhone = order.user?.phone ||
                      order.customer?.phone ||
                      order.shippingAddress?.phone ||
                      order.customerPhone ||
                      '-'
```

## الاختبار

1. شغل السيرفر
2. افتح صفحة الطلبات
3. شوف البيانات في Console
4. تأكد من الحقول الصحيحة

---

**الحالة:** 🔍 قيد التشخيص
