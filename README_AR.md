# متجر أبعاد التواصل 🛍️

## نظرة عامة
متجر إلكتروني متكامل لبيع الهواتف والإكسسوارات مع لوحة تحكم قوية للإدارة.

---

## ✨ المميزات الرئيسية

### للعملاء
- 🏠 صفحة رئيسية ديناميكية قابلة للتخصيص
- 📱 عرض المنتجات مع الفلترة والبحث
- 🛒 سلة تسوق متقدمة
- 💳 طرق دفع متعددة (Tap, Tabby, Tamara, MyFatoorah)
- 👤 صفحة بروفايل شخصية كاملة
- 📦 تتبع الطلبات
- ❤️ قائمة المفضلة
- 📍 إدارة العناوين

### للإدارة
- 📊 لوحة تحكم شاملة
- 📦 إدارة المنتجات (إضافة، تعديل، حذف)
- 🛍️ إدارة الطلبات
- 👥 إدارة العملاء
- 🏗️ بناء الصفحة الرئيسية (Homepage Builder)
- 💰 إعدادات طرق الدفع
- ⚙️ إعدادات عامة

---

## 🛠️ التقنيات المستخدمة

### Frontend
- **Next.js 14** - React Framework
- **TypeScript** - Type Safety
- **Tailwind CSS** - Styling
- **React Icons** - Icons
- **Context API** - State Management

### Backend
- **Node.js** - Runtime
- **Express** - Web Framework
- **MongoDB** - Database
- **JWT** - Authentication
- **TypeScript** - Type Safety

---

## 📁 هيكل المشروع

```
hotw/
├── frontend/                 # تطبيق Next.js
│   ├── src/
│   │   ├── app/             # صفحات التطبيق
│   │   │   ├── account/     # صفحة البروفايل ✅
│   │   │   ├── admin/       # لوحة التحكم
│   │   │   │   ├── customers/      # إدارة العملاء
│   │   │   │   ├── products/       # إدارة المنتجات
│   │   │   │   ├── orders/         # إدارة الطلبات
│   │   │   │   ├── settings/       # الإعدادات
│   │   │   │   │   └── payment/    # إعدادات الدفع ✅
│   │   │   ├── cart/        # السلة
│   │   │   ├── checkout/    # إتمام الطلب
│   │   │   └── products/    # المنتجات
│   │   ├── components/      # المكونات
│   │   ├── contexts/        # Context API
│   │   └── hooks/           # Custom Hooks
│   └── public/              # الملفات الثابتة
│
├── backend/                 # خادم Express
│   ├── src/
│   │   ├── controllers/     # Controllers
│   │   ├── middleware/      # Middleware
│   │   ├── routes/          # API Routes
│   │   ├── services/        # Business Logic
│   │   └── types/           # TypeScript Types
│   └── prisma/              # Database Schema
│
└── docs/                    # التوثيق
    ├── FIXES_COMPLETE.md    # تقرير الإصلاحات ✅
    └── TEST_FIXES.md        # دليل الاختبار ✅
```

---

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js 18+
- MongoDB
- npm أو yarn

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd hotw
```

### 2. تثبيت Backend
```bash
cd backend
npm install
```

### 3. إعداد ملف .env للـ Backend
```env
DATABASE_URL=mongodb://localhost:27017/abaad_store
JWT_SECRET=your-secret-key-here
PORT=5000
FRONTEND_URL=http://localhost:3000
```

### 4. تشغيل Backend
```bash
npm run dev
```

### 5. تثبيت Frontend
```bash
cd ../frontend
npm install
```

### 6. إعداد ملف .env للـ Frontend
```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

### 7. تشغيل Frontend
```bash
npm run dev
```

### 8. فتح المتصفح
- الموقع: `http://localhost:3000`
- لوحة التحكم: `http://localhost:3000/admin`

---

## 👤 الحسابات الافتراضية

### حساب أدمن
```
البريد: admin@abaad.com
كلمة المرور: admin123
```

### حساب عميل
```
البريد: customer@example.com
كلمة المرور: customer123
```

---

## 📖 الصفحات الرئيسية

### للعملاء
| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الرئيسية | `/` | الصفحة الرئيسية الديناميكية |
| المنتجات | `/products` | عرض جميع المنتجات |
| تفاصيل المنتج | `/products/[id]` | تفاصيل منتج معين |
| السلة | `/cart` | سلة التسوق |
| إتمام الطلب | `/checkout` | صفحة الدفع |
| البروفايل | `/account` | الملف الشخصي ✅ |
| تسجيل الدخول | `/login` | تسجيل دخول العملاء |

### للإدارة
| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| لوحة التحكم | `/admin` | الصفحة الرئيسية |
| المنتجات | `/admin/products` | إدارة المنتجات |
| الطلبات | `/admin/orders` | إدارة الطلبات |
| العملاء | `/admin/customers` | إدارة العملاء |
| بناء الصفحة | `/admin/homepage-builder` | تخصيص الصفحة الرئيسية |
| الإعدادات | `/admin/settings` | الإعدادات العامة |
| إعدادات الدفع | `/admin/settings/payment` | طرق الدفع ✅ |
| تسجيل الدخول | `/admin/login` | تسجيل دخول الأدمن |

---

## 🔐 الأمان والحماية

### المصادقة
- JWT Tokens للمصادقة
- Refresh Tokens للجلسات الطويلة
- تشفير كلمات المرور (bcrypt)

### الصلاحيات
- **USER** - العملاء العاديين
- **ADMIN** - المديرين

### الحماية
- Middleware للتحقق من التوكنات
- Middleware للتحقق من صلاحيات الأدمن
- CORS محمي
- Rate Limiting
- Input Validation

---

## 💳 طرق الدفع المدعومة

### 1. Tap Payment (تاب للدفع)
- بطاقات الائتمان
- Apple Pay
- Google Pay

### 2. Tabby (تابي)
- الدفع بالتقسيط
- اشتر الآن وادفع لاحقاً

### 3. Tamara (تمارا)
- الدفع بالتقسيط
- اشتر الآن وادفع لاحقاً

### 4. MyFatoorah (مدفوع)
- بطاقات الائتمان
- محافظ إلكترونية
- تحويل بنكي

---

## 🎨 التخصيص

### تغيير الألوان
عدّل ملف `frontend/tailwind.config.ts`:
```typescript
colors: {
  primary: {
    50: '#f0f9ff',
    100: '#e0f2fe',
    // ... المزيد
    600: '#0284c7', // اللون الأساسي
    // ... المزيد
  }
}
```

### تغيير الشعار
استبدل الملف: `frontend/public/logo.svg`

### تغيير الخطوط
عدّل ملف `frontend/src/app/layout.tsx`

---

## 📊 API Endpoints

### المصادقة
```
POST   /api/auth/register      - تسجيل حساب جديد
POST   /api/auth/login         - تسجيل الدخول
GET    /api/auth/profile       - جلب بيانات المستخدم
PUT    /api/auth/profile       - تحديث بيانات المستخدم
```

### المنتجات
```
GET    /api/products           - جلب جميع المنتجات
GET    /api/products/:id       - جلب منتج معين
POST   /api/products           - إضافة منتج (أدمن)
PUT    /api/products/:id       - تعديل منتج (أدمن)
DELETE /api/products/:id       - حذف منتج (أدمن)
```

### الطلبات
```
GET    /api/orders             - جلب طلبات المستخدم
GET    /api/orders/:id         - جلب طلب معين
POST   /api/orders             - إنشاء طلب جديد
PUT    /api/orders/:id         - تحديث حالة الطلب (أدمن)
```

### الدفع
```
GET    /api/payments/methods   - جلب طرق الدفع المفعلة
POST   /api/payments/create    - إنشاء عملية دفع
GET    /api/payments/settings  - جلب إعدادات الدفع (أدمن)
PUT    /api/payments/settings  - تحديث إعدادات الدفع (أدمن)
```

---

## 🧪 الاختبار

### اختبار Frontend
```bash
cd frontend
npm run test
```

### اختبار Backend
```bash
cd backend
npm run test
```

### اختبار شامل
راجع ملف `TEST_FIXES.md` للاختبارات التفصيلية

---

## 🐛 حل المشاكل الشائعة

### المشكلة: لا يمكن الاتصال بـ Backend
**الحل:**
1. تأكد من تشغيل Backend على المنفذ 5000
2. تحقق من ملف `.env` في Frontend
3. تأكد من عدم وجود Firewall يمنع الاتصال

### المشكلة: خطأ في قاعدة البيانات
**الحل:**
1. تأكد من تشغيل MongoDB
2. تحقق من `DATABASE_URL` في `.env`
3. جرب إعادة تشغيل MongoDB

### المشكلة: صفحة البروفايل لا تعمل
**الحل:**
1. تأكد من تسجيل الدخول
2. افحص Console في المتصفح
3. تحقق من وجود Token في localStorage

### المشكلة: إعدادات الدفع لا تحفظ
**الحل:**
1. تأكد من تسجيل الدخول كأدمن
2. افحص Network في أدوات المطور
3. تحقق من صلاحيات قاعدة البيانات

---

## 📝 التحديثات الأخيرة

### الإصدار 2.0 (27 نوفمبر 2025)
- ✅ إصلاح صفحة البروفايل بالكامل
- ✅ إضافة صفحة إعدادات الدفع للأدمن
- ✅ فصل صفحة العملاء عن البروفايل
- ✅ تحسين الأمان والحماية
- ✅ إصلاح جميع الأخطاء

---

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء Branch جديد
3. Commit التغييرات
4. Push إلى Branch
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License

---

## 📞 الدعم

للدعم والاستفسارات:
- البريد الإلكتروني: support@abaad.com
- الموقع: https://abaad.com
- الوثائق: راجع ملفات `FIXES_COMPLETE.md` و `TEST_FIXES.md`

---

## 🎉 شكراً لاستخدام متجر أبعاد التواصل!

**المشروع جاهز للاستخدام بشكل كامل! 🚀**
