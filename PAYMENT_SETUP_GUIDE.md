# دليل إعداد طرق الدفع 💳

## المشكلة
عند محاولة الدفع، تظهر رسالة خطأ: "طريقة الدفع غير متاحة"

## السبب
لا توجد طرق دفع مفعلة في قاعدة البيانات

---

## الحل السريع ⚡

### الطريقة 1: تفعيل الدفع عند الاستلام (COD)

هذه أسهل طريقة للاختبار:

```bash
cd backend
npx ts-node enable-test-payment.ts
```

هذا السكريبت سيقوم بـ:
- ✅ تفعيل الدفع عند الاستلام (COD)
- ✅ تفعيل Tap Payment بمفاتيح تجريبية
- ✅ عرض جميع طرق الدفع المتاحة

---

### الطريقة 2: من لوحة التحكم

1. سجل دخول كأدمن: `http://localhost:3000/admin/login`
2. اذهب إلى: `http://localhost:3000/admin/settings/payment`
3. فعّل أي طريقة دفع
4. أدخل المفاتيح (أو اتركها فارغة لـ COD)
5. احفظ التغييرات

---

## طرق الدفع المدعومة

### 1. الدفع عند الاستلام (COD) ✅ الأسهل
```json
{
  "provider": "cod",
  "enabled": true,
  "config": {
    "name": "الدفع عند الاستلام",
    "nameEn": "Cash on Delivery"
  }
}
```

**المميزات:**
- ✅ لا يحتاج مفاتيح API
- ✅ يعمل مباشرة
- ✅ مثالي للاختبار

---

### 2. Tap Payment
```json
{
  "provider": "tap",
  "enabled": true,
  "config": {
    "apiKey": "sk_test_...",
    "secretKey": "sk_test_...",
    "publicKey": "pk_test_..."
  }
}
```

**للحصول على المفاتيح:**
1. سجل في: https://tap.company
2. اذهب إلى Dashboard > API Keys
3. انسخ Test Keys

---

### 3. Tabby
```json
{
  "provider": "tabby",
  "enabled": true,
  "config": {
    "publicKey": "pk_test_...",
    "secretKey": "sk_test_...",
    "merchantId": "..."
  }
}
```

**للحصول على المفاتيح:**
1. سجل في: https://tabby.ai
2. اذهب إلى Dashboard > Developers
3. انسخ API Keys

---

### 4. Tamara
```json
{
  "provider": "tamara",
  "enabled": true,
  "config": {
    "apiKey": "...",
    "apiToken": "...",
    "merchantId": "..."
  }
}
```

**للحصول على المفاتيح:**
1. سجل في: https://tamara.co
2. اذهب إلى Dashboard > API
3. انسخ Credentials

---

### 5. MyFatoorah
```json
{
  "provider": "myfatoorah",
  "enabled": true,
  "config": {
    "apiKey": "..."
  }
}
```

**للحصول على المفاتيح:**
1. سجل في: https://myfatoorah.com
2. اذهب إلى Dashboard > API
3. انسخ API Key

---

## الاختبار

### 1. تفعيل طريقة الدفع
```bash
cd backend
npx ts-node enable-test-payment.ts
```

### 2. إنشاء طلب
1. افتح: `http://localhost:3000`
2. أضف منتجات للسلة
3. اذهب إلى السلة
4. اضغط "إتمام الطلب"

### 3. اختيار طريقة الدفع
1. أدخل معلومات الشحن
2. اضغط "التالي"
3. اختر "الدفع عند الاستلام" أو "مدفوع"
4. اضغط "إتمام الدفع"

### 4. التحقق
- ✅ يجب أن يتم إنشاء الطلب
- ✅ يجب أن يتم التوجيه لصفحة الدفع (أو صفحة النجاح لـ COD)
- ✅ يجب أن يظهر الطلب في لوحة التحكم

---

## حل المشاكل

### المشكلة: "طريقة الدفع غير متاحة"
**الحل:**
```bash
cd backend
npx ts-node enable-test-payment.ts
```

### المشكلة: "فشل في إنشاء عملية الدفع"
**الحل:**
1. تحقق من المفاتيح السرية
2. تأكد من استخدام Test Keys وليس Live Keys
3. تحقق من اتصال الإنترنت

### المشكلة: "الطلب غير موجود"
**الحل:**
1. تحقق من أن الطلب تم إنشاؤه في قاعدة البيانات
2. افحص Console في المتصفح
3. افحص Terminal للـ Backend

---

## التحديثات المضافة

### في Backend:

#### 1. دعم COD في PaymentService
```typescript
case 'cod':
  result = {
    success: true,
    paymentUrl: `${process.env.FRONTEND_URL}/orders/${data.orderId}?status=success&payment=cod`,
    transactionId: `COD-${Date.now()}`,
    method: 'cod',
  }
  break
```

#### 2. سكريبت تفعيل طرق الدفع
- `backend/enable-test-payment.ts`

### في Frontend:

#### 1. معالجة أخطاء أفضل في checkout
```typescript
const errorMessage = error?.message || error?.toString() || 'حدث خطأ أثناء معالجة الدفع'
setError(errorMessage)
```

---

## الخطوات التالية

### للتطوير:
1. ✅ استخدم COD للاختبار السريع
2. ✅ استخدم Test Keys من مزودي الدفع
3. ✅ اختبر جميع السيناريوهات

### للإنتاج:
1. ⚠️ استبدل Test Keys بـ Live Keys
2. ⚠️ فعّل Webhook URLs
3. ⚠️ اختبر في بيئة staging أولاً
4. ⚠️ راقب المعاملات

---

## ملاحظات مهمة

### الأمان:
- 🔒 لا تشارك المفاتيح السرية
- 🔒 استخدم متغيرات البيئة (.env)
- 🔒 لا ترفع المفاتيح إلى Git

### الاختبار:
- ✅ استخدم Test Keys دائماً في التطوير
- ✅ اختبر جميع حالات النجاح والفشل
- ✅ تحقق من Webhooks

### الإنتاج:
- ⚠️ استخدم HTTPS فقط
- ⚠️ فعّل Rate Limiting
- ⚠️ راقب المعاملات المشبوهة

---

## الدعم

إذا واجهت مشاكل:
1. افحص Console في المتصفح
2. افحص Terminal للـ Backend
3. تحقق من قاعدة البيانات
4. راجع وثائق مزود الدفع

---

## الخلاصة

✅ **تم إضافة دعم COD للاختبار السريع**
✅ **تم إصلاح معالجة الأخطاء**
✅ **تم إضافة سكريبت تفعيل طرق الدفع**

**الآن يمكنك اختبار الدفع بسهولة! 🎉**
