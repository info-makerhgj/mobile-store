import { MongoClient } from 'mongodb';
import dotenv from 'dotenv';

dotenv.config();

const mongoUrl = process.env.DATABASE_URL || 'mongodb://localhost:27017/abaad_store';

/**
 * إضافة جميع مدن السعودية مع سعر موحد
 */
async function setupAllSaudiCities() {
  console.log('🚀 إضافة جميع مدن السعودية...\n');

  const client = new MongoClient(mongoUrl);

  try {
    await client.connect();
    const db = client.db();

    // جميع مدن السعودية (أكثر من 100 مدينة)
    const saudiCities = [
      // المدن الرئيسية (مع جميع الأشكال الممكنة)
      'الرياض', 
      'جدة', 'جده',
      'مكة', 'مكه', 'مكة المكرمة', 'مكه المكرمه',
      'المدينة', 'المدينه', 'المدينة المنورة', 'المدينه المنوره',
      'الدمام', 'الخبر', 'الظهران',
      
      // منطقة الرياض
      'الخرج', 'الدرعية', 'الدرعيه', 'الدوادمي', 'المجمعة', 'المجمعه', 
      'القويعية', 'القويعيه', 'وادي الدواسر', 'الأفلاج', 'الافلاج',
      'الزلفي', 'شقراء', 'حوطة بني تميم', 'حوطه بني تميم', 'عفيف', 
      'السليل', 'ضرما', 'المزاحمية', 'المزاحميه',
      'رماح', 'ثادق', 'حريملاء', 'الحريق', 'الغاط', 'مرات',
      
      // المنطقة الشرقية
      'الأحساء', 'الاحساء', 'احساء', 'الهفوف',
      'القطيف', 'الجبيل', 'حفر الباطن', 
      'النعيرية', 'النعيريه', 'بقيق', 'رأس تنورة', 'راس تنوره',
      'الخفجي', 'أبقيق', 'ابقيق', 'العديد', 'قرية العليا',
      
      // منطقة مكة
      'الطائف', 'طائف', 'القنفذة', 'القنفذه', 'الليث', 
      'رابغ', 'خليص', 'الجموم', 'بحرة', 'بحره', 'الكامل',
      'رنية', 'رنيه', 'تربة', 'تربه', 'الخرمة', 'الخرمه', 
      'العرضيات', 'أضم', 'اضم', 'الموية', 'الموي��', 'ميسان',
      
      // منطقة المدينة
      'ينبع', 'ينبع البحر', 'ينبع الصناعية', 'ينبع الصناعيه',
      'العلا', 'المهد', 'بدر', 'خيبر', 'الحناكية', 'الحناكيه', 
      'العيص', 'وادي الفرع',
      
      // منطقة القصيم
      'بريدة', 'بريده', 'عنيزة', 'عنيزه', 'الرس', 'المذنب', 
      'البكيرية', 'البكيريه', 'البدائع', 'الأسياح', 'الاسياح', 'النبهانية', 'النبهانيه',
      'عيون الجواء', 'الشماسية', 'الشماسيه', 'عقلة الصقور', 'ضرية', 'ضريه', 
      'رياض الخبراء',
      
      // منطقة عسير
      'أبها', 'ابها', 'ابهاء', 'خميس مشيط', 'خميس مشيط', 'بيشة', 'بيشه', 
      'النماص', 'سراة عبيدة', 'سراة عبيده', 'محايل', 'محايل عسير',
      'رجال ألمع', 'رجال المع', 'ظهران الجنوب', 'تثليث', 
      'بللسمر', 'بللحمر', 'بلقرن', 'المجاردة', 'المجارده', 
      'أحد رفيدة', 'احد رفيده', 'تنومة', 'تنومه', 'طريب', 'الحرجة', 'الحرجه',
      
      // منطقة تبوك
      'تبوك', 'الوجه', 'ضباء', 'ضبا', 'تيماء', 'تيما', 
      'أملج', 'املج', 'حقل', 'البدع',
      
      // منطقة حائل
      'حائل', 'حايل', 'بقعاء', 'الغزالة', 'الغزاله', 'الشنان', 
      'السليمي', 'الحائط', 'الشملي', 'موقق',
      
      // منطقة الحدود الشمالية
      'عرعر', 'رفحاء', 'رفحا', 'طريف', 'العويقيلة', 'العويقيله',
      
      // منطقة جازان
      'جازان', 'جيزان', 'صبيا', 'صبياء', 'أبو عريش', 'ابو عريش', 
      'صامطة', 'صامطه', 'الدرب', 'بيش', 'فرسان', 'العارضة', 'العارضه',
      'الريث', 'ضمد', 'الحرث', 'الداير', 'فيفا', 'فيفاء', 
      'العيدابي', 'هروب',
      
      // منطقة نجران
      'نجران', 'شرورة', 'شروره', 'حبونا', 'بدر الجنوب', 
      'يدمة', 'يدمه', 'ثار', 'خباش',
      
      // منطقة الباحة
      'الباحة', 'الباحه', 'بلجرشي', 'المخواة', 'المخواه', 'المندق', 
      'العقيق', 'قلوة', 'قلوه', 'القرى', 'الحجرة', 'الحجره',
      'غامد الزناد', 'بني حسن',
      
      // منطقة الجوف
      'سكاكا', 'القريات', 'القريا', 'دومة الجندل', 'دومه الجندل', 
      'طبرجل',
    ];

    console.log(`📍 عدد المدن: ${saudiCities.length} مدينة\n`);

    // جلب الشركات
    const providers = await db.collection('shipping_providers').find().toArray();
    
    if (providers.length === 0) {
      console.log('❌ لا توجد شركات شحن!');
      console.log('💡 شغّل أولاً: SETUP_SHIPPING.bat\n');
      return;
    }

    console.log(`✅ عدد الشركات: ${providers.length}\n`);

    // حذف الأسعار القديمة
    await db.collection('shipping_rates').deleteMany({});
    console.log('🗑️  تم حذف الأسعار القديمة\n');

    // أسعار مخصصة لكل شركة (يمكن تعديلها من إعدادات الشركة)
    const providerPrices: { [key: string]: { price: number; days: number } } = {
      'smsa': { price: 25, days: 2 },      // سمسا: 25 ريال، يومين
      'redbox': { price: 28, days: 2 },    // ريدبكس: 28 ريال، يومين
      'aramex': { price: 30, days: 3 },    // أرامكس: 30 ريال، 3 أيام
    };

    console.log('💰 أسعار الشركات:\n');
    Object.entries(providerPrices).forEach(([name, info]) => {
      const providerName = name === 'smsa' ? 'سمسا' : name === 'redbox' ? 'ريدبكس' : 'أرامكس';
      console.log(`   ${providerName}: ${info.price} ريال (${info.days} يوم)`);
    });
    console.log();

    let totalRates = 0;

    // إضافة سعر لكل مدينة مع كل شركة
    for (const city of saudiCities) {
      for (const provider of providers) {
        const providerConfig = providerPrices[provider.name] || { price: 30, days: 3 };
        
        await db.collection('shipping_rates').insertOne({
          providerId: provider._id,
          city: city,
          price: providerConfig.price,
          estimatedDays: providerConfig.days,
          createdAt: new Date(),
          updatedAt: new Date(),
        });
        totalRates++;
      }
    }

    console.log('✅ تم إضافة الأسعار:\n');
    console.log(`   📦 عدد المدن: ${saudiCities.length}`);
    console.log(`   🏢 عدد الشركات: ${providers.length}`);
    console.log(`   💵 إجمالي الأسعار: ${totalRates}\n`);

    // حفظ أسعار الشركات في إعداداتها
    for (const provider of providers) {
      const providerConfig = providerPrices[provider.name] || { price: 30, days: 3 };
      
      await db.collection('shipping_providers').updateOne(
        { _id: provider._id },
        {
          $set: {
            defaultPrice: providerConfig.price,
            defaultDays: providerConfig.days,
            updatedAt: new Date(),
          },
        }
      );
    }

    console.log('='.repeat(70));
    console.log('✅ تم إضافة جميع مدن السعودية بنجاح!');
    console.log('='.repeat(70));
    console.log();

    console.log('📋 الخطوات التالية:\n');
    console.log('1️⃣  فعّل شركة شحن من لوحة التحكم');
    console.log('   http://localhost:3000/admin/settings/shipping\n');
    
    console.log('2️⃣  لتغيير سعر شركة معينة:');
    console.log('   - افتح: http://localhost:3000/admin/settings/shipping');
    console.log('   - أو عدّل في قاعدة البيانات:\n');
    console.log('   // تغيير سعر سمسا لجميع المدن');
    console.log('   db.shipping_rates.updateMany(');
    console.log('     { providerId: ObjectId("...") },');
    console.log('     { $set: { price: 20 } }');
    console.log('   )\n');
    
    console.log('3️⃣  اختبر الآن:');
    console.log('   - سمسا: 25 ريال ✅');
    console.log('   - ريدبكس: 28 ريال ✅');
    console.log('   - أرامكس: 30 ريال ✅\n');

  } catch (error) {
    console.error('❌ خطأ:', error);
    process.exit(1);
  } finally {
    await client.close();
  }
}

setupAllSaudiCities();
