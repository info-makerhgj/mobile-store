# 🔧 إصلاح Environment Variables في Vercel

## المشكلة:
Frontend على Vercel يحاول الاتصال بـ API خطأ أو قديم

## الحل:

### 1️⃣ اذهب لـ Vercel Dashboard
افتح: https://vercel.com/dashboard

### 2️⃣ افتح مشروعك
اضغط على مشروع Frontend (mobile-store-frontend أو اسم مشابه)

### 3️⃣ اذهب للإعدادات
- اضغط على **Settings** (في الأعلى)
- من القائمة الجانبية اختر **Environment Variables**

### 4️⃣ ابحث عن المتغير
ابحث عن: `NEXT_PUBLIC_API_URL`

### 5️⃣ تحقق من القيمة
**يجب أن تكون:**
```
https://api.ab-tw.com/api
```

**إذا كانت مختلفة:**
- اضغط على الثلاث نقاط (...) بجانب المتغير
- اختر **Edit**
- غير القيمة إلى: `https://api.ab-tw.com/api`
- اضغط **Save**

**إذا مو موجودة أصلاً:**
- اضغط **Add New**
- Name: `NEXT_PUBLIC_API_URL`
- Value: `https://api.ab-tw.com/api`
- Environment: اختر **Production**, **Preview**, **Development** (الثلاثة)
- اضغط **Save**

### 6️⃣ أعد النشر (Redeploy)
**مهم جداً!** التغييرات ما تطبق إلا بعد Redeploy:

1. اذهب لـ **Deployments** (في الأعلى)
2. اضغط على آخر deployment (الأول في القائمة)
3. اضغط على الثلاث نقاط (...) في الأعلى
4. اختر **Redeploy**
5. اضغط **Redeploy** مرة ثانية للتأكيد

### 7️⃣ انتظر
- انتظر 1-2 دقيقة حتى ينتهي الـ deployment
- لما يصير **Ready** افتح موقعك

### 8️⃣ اختبر
افتح موقعك على Vercel وشوف إذا الأخطاء راحت!

---

## ملاحظات مهمة:

✅ **لازم تكون القيمة بالضبط:**
```
https://api.ab-tw.com/api
```

❌ **مو:**
- `https://api.ab-tw.com` (بدون /api)
- `http://api.ab-tw.com/api` (http بدون s)
- `https://your-backend-url.com` (القيمة القديمة)

✅ **لازم Redeploy بعد أي تغيير!**

---

## إذا ما اشتغل:

### تحقق من CORS:
Backend لازم يسمح لـ Vercel domain. شوف ملف `backend/src/server.ts`:

```typescript
const allowedOrigins = [
  'http://localhost:3000',
  'https://mobile-store-frontend-nu.vercel.app',  // ← تأكد من domain صحيح
  process.env.FRONTEND_URL
]
```

إذا domain مختلف، لازم نضيفه!

---

**أخبرني بعد ما تخلص!** 🚀
