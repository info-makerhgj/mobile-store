# 🔧 تحديث جميع صفحات الإدارة

## ✅ تم التحديث

- [x] `/admin/deals` - صفحة العروض
- [x] `/admin/products` - صفحة المنتجات

## 📋 يحتاج تحديث

لتحديث باقي الصفحات، استبدل الكود القديم بهذا:

### 1. في بداية الملف:
```typescript
// احذف
import { FiHome, FiPackage, ... } from 'react-icons/fi'

// أضف
import AdminLayout from '@/components/admin/AdminLayout'
```

### 2. في الـ return:
```typescript
// احذف
return (
  <div className="min-h-screen bg-gray-50 flex" dir="rtl">
    <aside ...>
      {/* القائمة الجانبية القديمة */}
    </aside>
    <div className="flex-1">
      <header ...>
        {/* الهيدر القديم */}
      </header>
      <main>
        {/* المحتوى */}
      </main>
    </div>
  </div>
)

// استبدل بـ
return (
  <AdminLayout>
    <div className="p-6">
      {/* المحتوى فقط */}
    </div>
  </AdminLayout>
)
```

## 📝 الصفحات المتبقية

1. `/admin/orders/page.tsx`
2. `/admin/customers/page.tsx`
3. `/admin/settings/page.tsx`
4. `/admin/homepage-builder/page.tsx`

## 💡 الفائدة

- ✅ قائمة جانبية موحدة في جميع الصفحات
- ✅ تشمل جميع الأقسام (بما فيها العروض 🔥)
- ✅ تصميم موحد
- ✅ سهولة الصيانة

---

**ملاحظة**: صفحة المنتجات والعروض الآن تستخدم AdminLayout الموحد وتعرض جميع الأقسام بشكل صحيح!
