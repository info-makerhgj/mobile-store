# 🚀 دليل الرفع الكامل - خطوة بخطوة

## 📌 الخطوة 1: تنظيف السيرفر القديم

### على جهازك (Windows):

1. **افتح PowerShell أو CMD**

2. **اتصل بالسيرفر:**
```bash
ssh ubuntu@3.72.52.243
```

3. **ارفع سكريبت التنظيف:**
```bash
# على جهازك (في مجلد المشروع)
scp clean-server.sh ubuntu@3.72.52.243:~/
```

4. **على السيرفر، نفذ السكريبت:**
```bash
chmod +x clean-server.sh
./clean-server.sh
```

5. **تأكد من التنظيف:**
```bash
ls -la ~
pm2 list
df -h
```

---

## 📌 الخطوة 2: رفع المشروع على GitHub

### على جهازك:

1. **شغل السكريبت:**
```bash
upload-to-github.bat
```

2. **اذهب إلى GitHub:**
   - افتح: https://github.com/new
   - اسم الـ Repository: `mobile-store` (أو أي اسم تحبه)
   - اختر: **Private** (إذا تبغاه خاص)
   - **لا تضف** README أو .gitignore
   - اضغط **Create repository**

3. **انسخ رابط الـ Repository** (مثلاً: `https://github.com/username/mobile-store.git`)

4. **ارجع للـ CMD/PowerShell ونفذ:**
```bash
git remote add origin https://github.com/YOUR-USERNAME/mobile-store.git
git push -u origin main
```

5. **تأكد من الرفع:**
   - افتح الـ repository على GitHub
   - تأكد من وجود مجلدات `frontend` و `backend`

---

## 📌 الخطوة 3: رفع Backend على السيرفر النظيف

### على السيرفر:

1. **استنسخ المشروع من GitHub:**
```bash
cd ~
git clone https://github.com/YOUR-USERNAME/mobile-store.git
cd mobile-store
```

2. **إعداد Backend:**
```bash
cd backend
npm install
```

3. **إنشاء ملف .env:**
```bash
nano .env
```

أضف المحتوى التالي:
```env
PORT=4000
DATABASE_URL=mongodb://localhost:27017/mobile-store
JWT_SECRET=your-super-secret-key-change-this-now
NODE_ENV=production
FRONTEND_URL=https://your-app.vercel.app

# إذا عندك Tap Payment
TAP_SECRET_KEY=your-tap-secret-key
TAP_PUBLIC_KEY=your-tap-public-key
```

احفظ بـ: `Ctrl+X` ثم `Y` ثم `Enter`

4. **Build وتشغيل:**
```bash
npm run build
pm2 start dist/server.js --name mobile-store-api
pm2 save
pm2 startup
```

5. **اختبر Backend:**
```bash
curl http://localhost:4000
```

يجب أن ترى رسالة نجاح!

6. **إعداد Nginx:**
```bash
sudo nano /etc/nginx/sites-available/mobile-store
```

أضف:
```nginx
server {
    listen 80;
    server_name 3.72.52.243;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/mobile-store /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

7. **اختبر من جهازك:**
```bash
curl http://3.72.52.243
```

---

## 📌 الخطوة 4: رفع Frontend على Vercel

### على جهازك:

1. **اذهب إلى Vercel:**
   - افتح: https://vercel.com
   - سجل دخول بحساب GitHub

2. **استيراد المشروع:**
   - اضغط **New Project**
   - اختر الـ repository: `mobile-store`
   - اضغط **Import**

3. **إعدادات المشروع:**
   - **Root Directory**: اختر `frontend`
   - **Framework Preset**: Next.js (سيختار تلقائياً)
   - **Build Command**: `npm run build`
   - **Output Directory**: `.next`

4. **Environment Variables:**
   اضغط **Add Environment Variable** وأضف:
   ```
   Name: NEXT_PUBLIC_API_URL
   Value: http://3.72.52.243
   ```

5. **اضغط Deploy** وانتظر...

6. **بعد النشر:**
   - انسخ رابط Vercel (مثلاً: `https://mobile-store-xyz.vercel.app`)

---

## 📌 الخطوة 5: تحديث CORS في Backend

### على السيرفر:

1. **عدل ملف .env:**
```bash
cd ~/mobile-store/backend
nano .env
```

2. **حدث FRONTEND_URL:**
```env
FRONTEND_URL=https://mobile-store-xyz.vercel.app
```

3. **أعد تشغيل Backend:**
```bash
pm2 restart mobile-store-api
```

---

## 📌 الخطوة 6: الاختبار النهائي

### اختبر كل شي:

1. **Backend:**
```bash
curl http://3.72.52.243
```

2. **Frontend:**
   - افتح: `https://mobile-store-xyz.vercel.app`
   - تأكد من ظهور الصفحة الرئيسية

3. **الاتصال بين Frontend و Backend:**
   - جرب تسجيل الدخول
   - جرب عرض المنتجات
   - افتح Console في المتصفح وتأكد من عدم وجود أخطاء

---

## 🔧 أوامر مفيدة

### على السيرفر:

```bash
# عرض حالة PM2
pm2 list

# عرض اللوجات
pm2 logs mobile-store-api

# إعادة التشغيل
pm2 restart mobile-store-api

# عرض حالة Nginx
sudo systemctl status nginx

# عرض حالة MongoDB
sudo systemctl status mongod

# عرض المساحة المتوفرة
df -h
```

---

## ❌ حل المشاكل الشائعة

### 1. Backend لا يعمل:
```bash
pm2 logs mobile-store-api
# اقرأ الأخطاء وصلحها
```

### 2. MongoDB لا يعمل:
```bash
sudo systemctl start mongod
sudo systemctl status mongod
```

### 3. Nginx لا يعمل:
```bash
sudo nginx -t
sudo systemctl restart nginx
```

### 4. Frontend لا يتصل بـ Backend:
- تأكد من `NEXT_PUBLIC_API_URL` في Vercel
- تأكد من CORS في Backend
- افتح Console في المتصفح

### 5. Port 4000 مغلق:
```bash
# على السيرفر
sudo ufw allow 4000
sudo ufw status
```

---

## 📊 التحديثات المستقبلية

### عند تحديث الكود:

1. **على جهازك:**
```bash
git add .
git commit -m "وصف التحديث"
git push
```

2. **على السيرفر (Backend):**
```bash
cd ~/mobile-store
git pull
cd backend
npm install
npm run build
pm2 restart mobile-store-api
```

3. **Frontend:**
   - Vercel سيحدث تلقائياً من GitHub! 🎉

---

## ✅ Checklist

- [ ] تنظيف السيرفر القديم
- [ ] رفع المشروع على GitHub
- [ ] رفع Backend على السيرفر
- [ ] Backend يعمل ويستجيب
- [ ] MongoDB يعمل
- [ ] Nginx يعمل
- [ ] رفع Frontend على Vercel
- [ ] Frontend يفتح بدون أخطاء
- [ ] الاتصال بين Frontend و Backend يعمل
- [ ] تسجيل الدخول يعمل
- [ ] المنتجات تظهر
- [ ] السلة تعمل

---

## 🎉 انتهى!

المشروع الآن:
- ✅ Backend على AWS: `http://3.72.52.243`
- ✅ Frontend على Vercel: `https://your-app.vercel.app`
- ✅ Database على السيرفر
- ✅ جاهز للاستخدام!

---

## 📞 للمساعدة

إذا واجهت أي مشكلة:
1. اقرأ اللوجات: `pm2 logs mobile-store-api`
2. تحقق من Console في المتصفح
3. تأكد من Environment Variables
4. تأكد من CORS
